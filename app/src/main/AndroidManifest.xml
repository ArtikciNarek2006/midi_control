<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    
    <uses-feature
        android:name="android.software.midi"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MidiControl"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        TODO: Create midi synth service -->
        <!--  MidiSynthService definition  -->
<!--        <service-->
<!--            android:name="com.midi_control.midi.services.MidiSynthService"-->
<!--            android:exported="true"-->
<!--            android:permission="android.permission.BIND_MIDI_DEVICE_SERVICE">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.media.midi.MidiDeviceService" />-->
<!--            </intent-filter>-->
<!--            <meta-data-->
<!--                android:name="android.media.midi.MidiDeviceService"-->
<!--                android:resource="@xml/midi_synth_service_device_info" />-->
<!--        </service>-->

        <!--  MidiLiveVisualizerService definition  -->
        <service
            android:name="com.midi_control.midi.services.MidiLiveVisualizerService"
            android:exported="true"
            android:permission="android.permission.BIND_MIDI_DEVICE_SERVICE"
            >
            <intent-filter>
                <action android:name="android.media.midi.MidiDeviceService" />
            </intent-filter>
            <meta-data
                android:name="android.media.midi.MidiDeviceService"
                android:resource="@xml/midi_live_vis_service_device_info" />
        </service>

    </application>

</manifest>